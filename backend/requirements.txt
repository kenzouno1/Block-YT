# Python Dependencies for YouTube Blocker
#
# For Ubuntu 22.04+ (uses PEP 668), install via apt:
#   sudo apt-get install python3-flask python3-flask-cors python3-requests
#
# For older systems or if apt packages not available, use pip:
#   pip3 install flask flask-cors requests
#
# Required packages:
flask>=3.0.0
flask-cors>=4.0.0
requests>=2.31.0
